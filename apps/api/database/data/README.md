# Database Data Folder

This folder is intentionally empty.

Do not commit production content, demo content, old import data, or public CMS JSON files here. Content is stored in the database and managed manually from apanel.

The default `db:seed` command seeds only core system access data through `CoreSystemSeeder`.
